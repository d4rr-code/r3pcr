# Virtual Environment
venv/

# Environment Variables
.env

# Python
__pycache__/
*.pyc
*.pyo

# Django
*.log
db.sqlite3
*.sqlite3
media/

# Local development (dev-only settings + disposable local database)
config/settings_local.py
local_dev.sqlite3

# Working artifacts (manuscripts, scratch — never source)
paper_revisions/
tmp/

# Collected static (regenerated by `collectstatic` on deploy — not source)
staticfiles/

# VS Code
.vscode/

# Virtual environment (alternate name)
.venv/

# Claude Code internals
.claude/

# Windows line ending artifacts
*.orig

# Node.js dependencies (regenerated from package.json — never source)
node_modules/
.pnpm-store/
npm-debug.log*
pnpm-debug.log*

# OCR debug output (images extracted from SAD PDFs during development — not source)
sad_page_images/

# Local analysis / research scripts (not part of the app — keep a backup elsewhere)
analyze_shipment_monitoring.py
inspect_capstone_respondents.py
inspect_survey_questionnaire.py
shipment_deep_analysis.py
shipment_monitoring_analysis.json